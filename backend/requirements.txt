# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
motor>=3.4.0           # async MongoDB driver
pymongo>=4.7.0

# Auth / security
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0
python-multipart>=0.0.9   # required for OAuth2PasswordRequestForm
redis>=5.0.0                # optional; MFA rate limits fall back to in-process if REDIS_URL unset

# Config / validation
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0
# Explicit: OpenAPI generation imports EmailStr → pydantic checks metadata for this dist;
# if missing, /openapi.json returns 500 with detail "email-validator".
email-validator>=2.0.0,<3
eval-type-backport>=0.2.0  # Pydantic + Python 3.9: evaluate str | None style hints

# MFA (TOTP) — stub ready, wire in production
pyotp>=2.9.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Manual SOW — document parsing & export
pypdf>=4.0.0
python-docx>=1.1.0
reportlab>=4.0.0

# Manual SOW — optional AI tech stack (requires OPENAI_API_KEY)
openai>=1.40.0
